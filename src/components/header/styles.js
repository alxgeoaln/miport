const styles = (path) => ({
  dot: {
    width: 7,
    height: 7,
    borderRadius: 10,
    backgroundColor: path === '/' ? '#000' : '#fff',
    marginRight: 10,
    marginLeft: 10,
  },
})

export default styles
